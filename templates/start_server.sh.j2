#!/usr/bin/env bash
# Run as root - launched from upstart init or systemd service

sleep 10

sudo -u {{ os_user }} -H bash -c "source {{ diaspora_home }}/.bash_profile && cd {{ diaspora_home }}/diaspora && rvm use `cat {{ diaspora_home }}/diaspora/.ruby-version`@diaspora && DB=postgres RAILS_ENV=production ./script/server"


